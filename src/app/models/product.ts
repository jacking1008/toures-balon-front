import { CurrencyFormat } from '../global/currency-format';

export class Product {

    id_product: number;
    title: string;
    description: string;
    image: string;
    id_provider: string;
    category: string;
    status: boolean;
    user_rating: number;
    price: number;
    
}