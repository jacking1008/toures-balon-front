import { CurrencyFormat } from '../global/currency-format';
import { CardInfo } from './card-info';

export class Show {
    image: string;
    name: string;
    description: string;
    category: string;
    price: number[];
}
